<h1>$word.getName()</h1>
<ul>
  #foreach ($definition in $word.getDefinitions())
    <li>$definition.getDefinition()</li>
  #end
</ul>

<form action="/word/$word.getId()" method="post">
  <label for="anotherDefinition">Add a definition</label>
  <input id="anotherDefinition" name="anotherDefinition" type="text"></input>
  <hr>
  <button type="submit" class="btn btn-success">Add entry</button>
</form>

<h4><a href="/">Homepage</a></h4>
